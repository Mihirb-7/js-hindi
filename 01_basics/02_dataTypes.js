// earlier in js classes,obj , modeles etc is not wrritten but nowadays new standards is in use
"use strict" //treat all JS code as new version ,, it is by default nowadays

// alert("hello")   // we are using nodejs , not browser

let name = "usjhkd" //string
let age = 89 // no
let isLoggedIn = false; //bool

//null = standalone value
//undefiened = value not defined

//symbol = used to find uniqueness

console.log(typeof age);
console.log(typeof null);  // its output is object  IMPORTANT ASK IN INTERVIEW
console.log(typeof undefined);  // its output is undefiened
